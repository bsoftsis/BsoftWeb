@model IEnumerable<BsoftWeb.Models.Compra>

@{
    ViewBag.Title = "Listado Compra";
}

<h2>Lista Compra</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fechaCompra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nroComprobante)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechaRegistro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proveedor.razonSocial)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.nombreUsuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fechaCompra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nroComprobante)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaRegistro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Proveedor.razonSocial)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.nombreUsuario)
        </td>
        <td>
            <button type="button" class="btn-sm">
                @Html.ActionLink("Editar", "Edit", new { id = item.idCompra })
            </button>
            <button type="button" class="btn-sm">
                @Html.ActionLink("Detalle", "Details", new { id = item.idCompra })
            </button>
            <button type="button" class="btn-sm">
                @Html.ActionLink("Borrar", "Delete", new { id = item.idCompra })
            </button>
        </td>
    </tr>
}

</table>
