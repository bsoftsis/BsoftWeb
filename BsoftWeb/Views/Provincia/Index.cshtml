@model IEnumerable<BsoftWeb.Models.Provincia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreProvincia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreProvincia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idProvincia }) |
            @Html.ActionLink("Details", "Details", new { id=item.idProvincia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idProvincia })
        </td>
    </tr>
}

</table>
